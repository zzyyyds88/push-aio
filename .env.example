# push-aio 环境变量配置示例
#
# 使用方法：
# 1. 复制本文件为 .env：cp .env.example .env
# 2. 填入真实 API Key（任意长字符串，建议 >= 32 位随机字符）
# 3. .env 已在 .gitignore 中，不会被上传到 git
#
# 生成随机 Key 的方法（PowerShell）：
#   -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
# 生成随机 Key 的方法（Python）：
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

# 全局 API Key：同一把 Key 同时用于
#   - 外部程序调用：POST /api/notify
#   - WebUI 管理：/admin/api/*（前端首次访问弹窗输入并存浏览器 localStorage）
# 除 GET /api/health 外所有接口都需要在请求头携带
#   X-API-Key: <你填的值>
PUSH_AIO_API_KEY=change-me-to-a-random-string
