services:
  pushhub:
    image: pushhub:latest
    container_name: pushhub
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
